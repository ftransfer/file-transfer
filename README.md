## Usage

### Install Dependencies

```
$ cd file-transfer
```
# using yarn or npm
```
$ yarn (or `npm install`)
```

### Use it

```
# development mode
$ yarn dev (or `npm run dev`)

# production build
$ yarn build (or `npm run build`)
```
